import { headers, cookies } from "next/headers";
export default function ProductsPage() {
  // const headerLists = headers();
  // const cookies = headerLists.get("cookie");
  // console.log(cookies);
  // const name = cookies().get("name");
  // const email = cookies().get("email");
  // console.log(name);
  // console.log(email);
  // cookies().set("age", 32);
  return (
    <div>
      <h1>Products</h1>
    </div>
  );
}
